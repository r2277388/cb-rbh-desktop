# Normalize all text files and store as LF in repo
* text=auto eol=lf

# Explicit scripts
*.sh  text eol=lf
*.py  text eol=lf
*.txt text eol=lf

# Git LFS
reprint_project/cache/saldet_cache.pkl filter=lfs diff=lfs merge=lfs -text

