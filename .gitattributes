# Automatically normalize line endings for all files
* text=auto

# Force LF line endings for scripts and text files
*.sh   text eol=lf
*.py   text eol=lf
*.txt  text eol=lf

# Use Git LFS for large cache file(s)
reprint_project/cache/saldet_cache.pkl filter=lfs diff=lfs merge=lfs -text
